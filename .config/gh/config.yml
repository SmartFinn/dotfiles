aliases:
    open: repo view --web
    ch: "!gh pr list | fzf --preview 'gh pr view {+1}' | cut -f1 | xargs gh pr checkout"
    diff: "!gh pr list | fzf --preview 'gh pr view {+1}' | cut -f1 | xargs gh pr diff"
